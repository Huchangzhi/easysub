name: opencode

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  opencode:
    if: |
      (contains(github.event.comment.body, '/oc') || contains(github.event.comment.body, '/opencode')) &&
      (github.event.comment.author_association == 'OWNER')
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false

      # 🔐 动态生成 opencode.json，所有敏感信息来自 Secrets
      - name: Create opencode.json
        env:
          MODEL_NAME: ${{ secrets.MY_PRIVATE_MODEL }}
          BASE_URL: ${{ secrets.MY_PRIVATE_URL }}
          API_KEY: ${{ secrets.MY_PRIVATE_KEY }}
        run: |
          # 使用 jq 或直接 cat 构建 JSON，注意转义
          cat > opencode.json <<EOF
          {
            "\$schema": "https://opencode.ai/config.json",
            "providers": {
              "myprovider": {
                "name": "My Custom API",
                "package": "@opencode-ai/ai/providers/openai-compatible",
                "options": {
                  "baseURL": "${BASE_URL}",
                  "apiKey": "${API_KEY}"
                },
                "models": {
                  "${MODEL_NAME}": {
                    "name": "${MODEL_NAME}",
                    "modelID": "${MODEL_NAME}"
                  }
                }
              }
            }
          }
          EOF

      - name: Run OpenCode
        uses: anomalyco/opencode/github@latest
        with:
          # 动态拼接：myprovider/ + 模型名（从 Secret 读取）
          model: myprovider/${{ secrets.MY_PRIVATE_MODEL }}